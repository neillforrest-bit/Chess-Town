import PortalDashboard from '@/components/PortalDashboard';

export default function ChessTownLanding() {
  return <main className="portal-shell portal-shell--compact">
    <PortalDashboard />
  </main>;
}
